# ZengQiang Enhanced 1.16.5

## 表格交互

- 点击表格正文或页面其他区域时，浮动表格控制面板会自动收起。
- 默认使用首行高亮和当前行整行高亮，编辑定位更清晰。
- 斑马纹为可选模式；启用后不叠加当前行颜色，避免两套色块冲突。

## 开源发行

- 发布前对源码、压缩包和三个 Release 附件执行隐私与密钥扫描。
- 发行包只包含 `main.js`、`manifest.json` 与 `styles.css`，不包含设置、笔记、缓存、备份或本地路径。
- 表格编辑器包含 GPL-3.0 代码，本发行版按 GPL-3.0-only 发布。
