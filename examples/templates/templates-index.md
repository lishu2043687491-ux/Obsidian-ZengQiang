# 内置示例模板

安装后请将本目录下的模板文件复制到你的库目录（默认 `.templates/`，可在插件设置中修改）：

- `5why.md` — 5 Why 分析表
- `六问法 pro 版.md` — 六问法结构化复盘表

复制示例：

```bash
mkdir -p .templates
cp examples/templates/5why.md "examples/templates/六问法 pro 版.md" .templates/
```
