# 1.0.5

面向 Obsidian 社区提交的版本（知识仓库测验通过后再推 GitHub）。

## 相对 1.0.3 / 1.0.4

- 合并 1.0.4：manifest 标点、Claudian 随机 deviceId（无 hostname）、内嵌 BLP 无 localStorage
- README：隐私与权限说明（Vault 枚举、剪贴板、伴生插件、deviceId）
- CSS：移除 `:has()` 规则块；`text-decoration-line` → `text-decoration`；T 工具栏样式标记行去掉 `!important`（提高选择器优先级）
- 伴生 mte：`text-decoration-line` → `text-decoration`（知识仓库内 `markdown-table-enhancer`）

## 测验

见 `changelog-2026-06-1.0.5-测验记录.md`。

## Release 附件

`main.js`、`manifest.json`、`styles.css`
